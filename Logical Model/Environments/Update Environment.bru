meta {
  name: Update Environment
  type: http
  seq: 4
}

put {
  url: {{base_url}}/resources/environments/:name
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

params:path {
  name: <env_name>
}

body:json {
  {
    "description": "updated description"
  }
}
