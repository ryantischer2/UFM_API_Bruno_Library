meta {
  name: Get Device Credentials
  type: http
  seq: 1
}

get {
  url: {{base_url}}/resources/systems/:dev_name/credentials
  body: none
  auth: inherit
}

params:path {
  dev_name: <device_name>
}

params:query {
  ~credential_types: SSH_Server
}
